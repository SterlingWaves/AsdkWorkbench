# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'WorkbenchTexture' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
  pod "Texture"
  # Pods for WorkbenchTexture

  target 'WorkbenchTextureTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WorkbenchTextureUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
